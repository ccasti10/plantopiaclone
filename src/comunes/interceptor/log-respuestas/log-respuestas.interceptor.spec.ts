import { LogRespuestasInterceptor } from './log-respuestas.interceptor';

describe('LogRespuestasInterceptor', () => {
  it('should be defined', () => {
    expect(new LogRespuestasInterceptor()).toBeDefined();
  });
});
